import React, {Component } from "react";
import { View, Text } from "react-native";

class Card extends Component {
    render() {
        return(
            <View>
                <Text>Single card component</Text>
            </View>
        );
    }
}

export default Card;